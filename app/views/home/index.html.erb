<ul>
  <%- @users.each do |user| %>
    <li>
      <%= "#{user.name} created a #{user.wishlists[0].name}
            wishlist with #{user.wishlists[0].wishes.count} wishes" %>
    </li>
  <% end %>
</ul>
